<?php
    // include_once('connect.php');
    // $conn = connect_db();
    // $conn->exec("CREATE DATABASE users");

    // $sql = "CREATE TABLE users.user_data (
    // id INT(11) AUTO_INCREMENT PRIMARY KEY,
    // email VARCHAR(255),
    // username VARCHAR(255),
    // user_password VARCHAR(255))";
    // $conn->exec($sql);
?>